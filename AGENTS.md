# DeFi PMA 연구 협업 지침

이 파일은 `/Users/sikk/dev/DeFi-paper-review`에서 수행하는 연구 설계와 문헌 검토에 적용한다.

## 1. 현재 진실의 출처

다음 우선순위로 현재 연구 상태를 판단한다.

1. 사용자가 이 작업공간에서 가장 최근에 명시적으로 확정한 결정
2. `research/DECISIONS.md`에서 `ACCEPTED`로 표시된 결정
3. 이 저장소의 현재 연구 문서와 데이터
4. 확인한 1차 문헌, 공식 문서, 재현 가능한 코드와 실험 결과
5. 과거 ChatGPT 답변, 첨부 대화, 다른 작업공간의 Codex 메모리

- 과거 답변과 메모리는 배경 자료일 뿐 현재 정의가 아니다.
- `/Users/sikk/dev/pma-taxonomy`, `/Users/sikk/Documents/ndss` 등 다른 작업공간의 PMA/AVE 결정을 자동으로 가져오지 않는다.
- `AVE`, `DeFiSense`, `Surface`, `Mode I/X`, `D/R/G` 등의 과거 용어는 이 저장소에서 다시 검증되어 `ACCEPTED`가 되기 전까지 미확정이다.
- 사용자가 아이디어를 논의하거나 긍정적으로 반응한 것을 확정으로 간주하지 않는다. 사용자가 명시적으로 확정하거나 결정 로그가 갱신된 경우에만 확정이다.

## 2. 연구 에이전트의 책임

- 사용자는 모든 기술적 주장의 옳고 그름을 직접 판별해야 하는 심사위원이 아니다. 근거 제시, 반례 탐색, 모순 검사, 불확실성 표시는 에이전트의 책임이다.
- 사용자의 지적에 즉시 동의해 주장을 바꾸지 않는다. 먼저 기존 주장, 지적된 문제, 증거, 반례를 대조한다.
- 답을 모르면 `UNKNOWN` 또는 `UNRESOLVED`로 남긴다. 그럴듯한 정의나 수식으로 빈칸을 채우지 않는다.
- 문헌이 말한 사실, 저장소에서 관측한 사실, 에이전트의 추론과 제안을 분리한다.
- 새로운 제안이 나와도 현재 설계를 조용히 덮어쓰지 않는다.

## 3. 변경 규칙

연구 개념과 방법론은 다음 상태 중 하나를 가진다.

- `PROPOSED`: 검토 전 제안
- `ACCEPTED`: 현재 기준으로 채택
- `REJECTED`: 검토 후 기각
- `SUPERSEDED`: 새 결정으로 대체됨
- `UNRESOLVED`: 증거 또는 정의 부족

`ACCEPTED` 항목을 변경하려면 `research/DECISIONS.md`에 아래 내용을 남긴다.

1. 기존 결정과 버전
2. 제기된 반례 또는 새 증거
3. 유지·수정·철회 중 판정
4. 새 결정
5. 정의, 그래프, 구현, 데이터, 평가, 논문 주장에 미치는 영향

Freeze는 영구 봉인이 아니다. 후속 반례로 다시 열 수 있지만, 반드시 버전과 영향을 기록한다.

## 4. 방법론 설계 원칙

- 공격자, 피해자, 공격 유형 라벨을 입력 전제로 삼지 않는다. 원시 실행 증거와 인과 경로를 구성한 뒤 역할을 파생한다.
- 방법론의 최소 인과 사슬은 다음 질문을 연결해야 한다.

  `행위자 후보 → 개입 → 상태 변화 → 왜곡된 경제값 → 소비 → 정산/의사결정 → 자산·부채 결과 → 수혜와 손실`

- `개입 지점`, `왜곡된 값`, `소비 지점`, `정산 지점`, `경제 결과`를 서로 다른 개념으로 유지한다.
- 주소의 고정 role보다 행위와 인과관계의 causal role을 우선한다. 한 주소가 여러 역할을 가질 수 있다.
- 원시 증거, semantic action 가설, 경제·인과 해석을 별도 계층으로 유지한다.
- Semantic action은 함수명이나 이벤트 whitelist만으로 정하지 않는다. 자산·청구권·부채·담보·권한을 변화시키거나, 경제값을 생성·소비하거나, 핵심 자금 조달·회수를 설명하는지로 포함 여부를 정한다.
- 복원할 수 없는 행위는 `UNKNOWN_ACTION`으로 보존한다.
- 실제 순자산 변화, 거래 P&L, 조작으로 인한 반사실 추가이익, 피해자의 반사실 손실을 구분한다.
- 공격자 이익과 피해자 손실이 항상 같다고 가정하지 않는다.
- 수익성만으로 공격을 판정하지 않는다. 정상 차익거래, 청산, 대형 swap, JIT liquidity 등 hard negative와 구별할 원리가 필요하다.

이 항목들은 proof obligation이다. 정확한 node/edge 목록, 수식, 시간 범위, reference value, 판정 임계값은 아직 확정된 것으로 간주하지 않는다.

## 5. 작업 순서와 단위

- 전체 요소를 서로 독립적으로 확정하지 않는다. 각 결정이 전체 인과 사슬에 미치는 영향을 함께 검토한다.
- 설명과 사용자 결정은 가능한 한 한 번에 하나의 작은 결정 단위로 진행한다.
- 설계 검증은 대표 사건 하나를 처음부터 끝까지 복원하는 vertical slice로 수행하고, 서로 다른 공격 family와 정상 반례에 같은 schema를 반복 적용한다.
- 단일 사례에 맞춰 schema를 확정하지 않는다.
- 구현 파이프라인을 먼저 만들지 않는다. 먼저 Methodology Kernel에서 정의, 관측 가능성, 증거 출처, 인과 경로, 계산, 판정 조건을 닫는다.
- 사용자가 요청하지 않은 광범위한 파일 편집이나 구현은 시작하지 않는다.
- 서브에이전트나 다중 역할은 사용자가 명시적으로 요청했을 때만 사용한다. 역할을 나누더라도 승인된 평가 기준 없이 다수 의견을 정답처럼 취급하지 않는다.

## 6. 단계별 통과 조건

각 단계는 문서 제목이 아니라 검증 가능한 산출물로 끝난다.

1. **Scope gate**: 연구 질문, 대상 PMA, 시간 범위, 비대상이 명시됨
2. **Definition gate**: 핵심 개념마다 정의, 필요조건, 반례, 미확정 항목이 있음
3. **Observation gate**: 각 사실에 `preferred source → fallback → 실패 시 영향`이 있음
4. **Causal gate**: 개입에서 경제 결과까지 연결되는 증거 경로가 있음
5. **Discrimination gate**: 공격 사례와 대응 hard negative를 구별함
6. **Calculation gate**: 이익·손실 계산이 수동 정답 또는 재현 가능한 반사실과 대조됨
7. **Evaluation gate**: 부분 과업과 end-to-end 평가가 분리됨

통과하지 못한 단계가 있으면 다음 단계의 확정본을 작성하지 않는다. 다만 막힌 원인을 찾기 위한 제한된 실험이나 사례 분석은 허용한다.

## 7. 증거와 문헌 규칙

- 기술적 주장에는 가능한 한 논문 원문, 공식 문서, 공식 코드·데이터 같은 1차 출처를 사용한다.
- 논문 초록이나 관련연구의 재서술만으로 세부 방법론을 단정하지 않는다.
- 모든 핵심 graph node와 edge에 실제 생성 근거를 연결한다. 이벤트만으로 데이터 의존성을 증명하지 않는다.
- 라벨 데이터는 ground truth 여부와 구축 절차를 검토하기 전까지 탐지 입력이나 정답으로 사용하지 않는다.
- 출처가 충돌하면 한쪽을 선택해 숨기지 말고 충돌과 선택 기준을 제시한다.

## 8. 대화와 산출물 형식

방법론 관련 답변은 필요에 따라 다음 구조를 사용한다.

1. 현재 검토하는 주장 또는 결정
2. 현재 상태: `PROPOSED / ACCEPTED / UNRESOLVED`
3. 근거와 관측 가능성
4. 반례와 실패 조건
5. 기존 결정에 미치는 영향
6. 다음 검증 또는 한 가지 결정 사항

- 단순히 “맞다”, “좋은 지적이다”라고 말하고 주장을 바꾸지 않는다.
- 사과나 동의보다 어떤 명제가 왜 유지·수정·철회되는지를 먼저 설명한다.
- 평이한 한국어와 실제 사건 예시를 사용한다. 약어와 추상적인 에이전트 용어를 남발하지 않는다.
- 문서를 수정한 뒤에는 채팅에서 핵심 변경과 아직 미확정인 부분을 설명한다.
- 사용자가 한 번에 전체 그림을 요구하면 통합 구조를 먼저 제시하되, 그 안의 개별 결정 상태를 구분한다.

## 9. 현재 작업 맥락

현재 맥락과 열린 문제는 `research/WORKING_CONTEXT.md`, 확정 및 변경 이력은 `research/DECISIONS.md`를 따른다.

